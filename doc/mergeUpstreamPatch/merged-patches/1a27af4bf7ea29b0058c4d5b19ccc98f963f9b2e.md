# 1a27af4b：snapshot 就绪前保持 NOT_SERVING

- 上游提交：`1a27af4bf7ea29b0058c4d5b19ccc98f963f9b2e`
- RTD 提交：`9238af193f7deabbd24e84ab57d48e14d435ca57`

## 基线关联

fork 基线在 consistent store 尚无 snapshot 时就把公开 gRPC service 标为 `SERVING`，健康状态与可读状态不一致。

## 移植与验证

公开 service 注册 readiness future；内部 reflection/health services 立即 ready，consistent service 在 `db.snapshots() >= 1` 后切换。

```text
cargo check -p rtd-indexer-alt-consistent-store
Finished successfully
```

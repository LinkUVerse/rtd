# 14172d38：epoch 切换不取消 effects 等待

- 上游提交：`14172d3847746cfd243242504a70c8b478fc8f9d`
- RTD 提交：`018490cd2bd164a6d21e3fbfa9bdfd7831dfa2f7`

## 基线关联

transaction orchestrator 把本地 effects 通知包在 `within_alive_epoch` 中。交易可在 epoch 边界后完成，旧逻辑会取消等待并最终返回 finality timeout。

## 移植与验证

effects 等待改为跨 epoch 存活，并严格校验通知结果数。

```text
cargo check -p rtd-core
cargo test -p rtd-core transaction_orchestrator --lib
5 passed; 0 failed
```

上游回归为 simtest；本机没有 `cargo-simtest`。

# f205eb2a：按指定版本读取 object funds

- 上游提交：`f205eb2a43fa368d5920549cf0842d7c79467b44`
- RTD 提交：`4f4b06a57ef4704acb51813039ad5c2b0ff82147`

## 基线关联

第 138 条已移植补丁让 scheduler 避免读取可能被 prune 的旧对象，但 object-funds 路径仍使用 latest account amount。执行交易已有 assigned accumulator version；读取更晚状态会使余额判断依赖节点当前缓存进度，而不是该交易确定的版本。

## 移植与验证

`AccountFundsRead` 新增精确版本读取，writeback cache 和测试 mock 均实现该接口；object-funds scheduler 使用交易的 accumulator version 读取并保留上游诊断日志。

```text
cargo test -p rtd-core funds_withdraw_scheduler --lib
39 passed; 0 failed; 611 filtered out

git diff --check
通过
```

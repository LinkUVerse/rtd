# 19158a94：保留 consensus sync 活性信号

- 上游提交：`19158a9471c7bb6dd6a77830fb7eb5bf958749ae`
- RTD 提交：`54095579a61cf769c81f167b2be1ea3b527e523f`

## 基线关联

fork 基线 `CommitSyncer` 拉取 certified-commit blocks 和 vote blocks 后未更新 `CommitVoteMonitor`，会延迟后续 commit sync 调度。block subscription 在请求范围没有缓存块时直接等待广播，即使本节点已经提议过块，也无法给恢复 peer 提供触发同步的证据。

## 移植与验证

仅抽取两个独立修复：对 commit-sync 拉取块补记 votes；订阅缓存窗口为空且存在非 genesis proposed block 时回送最后提议块。没有移植上游整套 `RoundTracker` 重构。

```text
cargo test -p consensus-core handle_subscribe_blocks --lib
1 passed; 0 failed

cargo test -p consensus-core commit_syncer --lib
2 passed; 0 failed

cargo check -p consensus-core
Finished successfully
```

# 5dc75293：拒绝 legacy package 依赖 modern package

- 上游提交：`5dc752931b986e56e55a1645d5ada984e0eafdb7`
- RTD 提交：`918ec3d8216d13f191ce2ffb4fe07cd8ed89117e`

## 基线关联

旧式 `Move.toml` 直接依赖现代 package 时无法表达现代依赖语义，但基线仍接受并继续构图。

## 红绿验证

测试先在“预期 legacy 加载失败”处 panic；增加直接依赖校验后通过。文档链接按品牌映射到 RTD migration 页面。

```text
cargo test --manifest-path external-crates/move/Cargo.toml -p move-package-alt legacy_depends_on_modern --lib
1 passed; 0 failed
```

# a817cc2d：修正 IPv6 端口测试断言

- 上游提交：`a817cc2d45f0419c367f9b4bc92558bbbfb3f5a5`
- RTD 提交：`c4fc569f1d8269ccb6280bf38bbe1eac7eadc23e`
- 上游标题：`fix: correct IPv6 port checks in multiaddr tests (#24640)`

## 基线关联

fork 基线 `crates/mysten-network/src/multiaddr.rs` 的两个 IPv6 测试分支都错误读取 `multi_addr_ip4.port()`。RTD 品牌替换后的 `crates/linku-network/src/multiaddr.rs` 保留了同一错误，因此该测试补丁直接适用。

## 移植内容

- 路径映射：`crates/mysten-network` -> `crates/linku-network`。
- `test_with_zero_ip` 和 `test_with_localhost_ip` 的 IPv6 端口断言改为读取 `multi_addr_ip6`。
- 未引入上游 `mysten.sui` 字符串；RTD 原有 `linku.rtd` 保持不变。

## 验证

```text
RTD_SKIP_SIMTESTS=1 cargo test -p linku-network test_with_ -- --nocapture
2 passed; 0 failed
```

# e58b99f6：module extension 缺失符号信息时不崩溃

- 上游提交：`e58b99f6ff34245ce9c12838576a46210b4c1640`
- RTD 提交：`85707e7e38446ec4b0fd6211a3cb930a6dc34c2a`

## 基线关联

module extension 编译支持和 analyzer 的 `def_info.unwrap()` 均存在于 fork 基线；依赖缓存过滤 extension definition 时 analyzer 会 panic。

## 移植与验证

仅在 definition 缺失时跳过该 use-def，并把只读 `DefMap` 引用改为 immutable；未移植编辑器版本 bump。

```text
cargo check -p rtd --bin rtd
Finished successfully
```

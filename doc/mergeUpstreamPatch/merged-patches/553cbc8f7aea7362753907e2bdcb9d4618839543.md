# 553cbc8f：启用 S3 ETag 条件写

- 上游提交：`553cbc8f7aea7362753907e2bdcb9d4618839543`
- RTD 提交：`e2f4d3df7fe0254cbbf30ac99636a6362e28f77a`

## 基线关联

fork 基线的 checkpoint blob indexer 使用默认 S3 builder，未配置 conditional put。并发更新对象或 watermark 时无法用当前 ETag 防止覆盖竞争。

## 移植与验证

S3 backend 显式配置 `S3ConditionalPut::ETagMatch`，其他 object-store backend 不变。

```text
cargo check -p rtd-checkpoint-blob-indexer
Finished `dev` profile successfully

rustfmt --edition 2024 --check crates/rtd-checkpoint-blob-indexer/src/main.rs
通过
```

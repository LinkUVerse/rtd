# 7ebaecc6：修复 React Router open-redirect XSS

- 上游提交：`7ebaecc69bb590916863234c5056bfbe20f3fe64`
- RTD 提交：`956156c695bd78a0c0500dcd66c3631b30483308`

## 基线关联

RTD 根 workspace 与 fork 基线均锁定 `@remix-run/router` 1.23.0，落在 GHSA-2w69-qvjg-hvjx 的受影响范围 `<=1.23.1`。

## 移植与验证

根 `pnpm.overrides` 强制升级到 `>=1.23.2`，lockfile 精确更新到 1.23.2。未吸收当前 pnpm 对 brand-renamed lock 节点产生的无关重排。

```text
pnpm install --lockfile-only --frozen-lockfile --ignore-scripts
exit 0

lockfile 检查：不存在 1.23.0，package/snapshot 均为 1.23.2
```

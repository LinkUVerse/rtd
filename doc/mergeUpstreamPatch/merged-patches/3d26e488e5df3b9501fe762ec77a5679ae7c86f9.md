# 3d26e488：snapshot restore 重试并快速失败

- 上游提交：`3d26e488e5df3b9501fe762ec77a5679ae7c86f9`
- RTD 提交：`c368185dcced0cafe3d48ab54dd8b63ef82a8a9a`

## 基线关联

formal snapshot transaction backfill 的 checkpoint 下载遇到瞬时 EOF 会立即终止；summary、snapshot、backfill 任一任务失败后，主流程仍可能长时间等待其他任务。

## 移植与验证

checkpoint 下载复用 `max_retries` 并线性退避，三个并行任务使用 `tokio::select!` 传播首个失败。

```text
cargo check -p rtd-tool
Finished successfully
```

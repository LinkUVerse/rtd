# 0daee96f：接受缩短的 balance owner 地址

- 上游提交：`0daee96fca471ccd79066774a7704aa23b5e215c`
- RTD 提交：`a2925c0ef7aeee40c259ecf6684d8786697fd975`

## 基线关联

fork 基线直接用 `RtdAddress` 解析 gRPC owner，因此拒绝 SDK/API 接受的 `0x2` 缩短形式。

## 移植与验证

先以回归测试确认 `0x2` 返回 `FIELD_INVALID`，再通过 `rtd_sdk_types::Address` 解析并转换为核心地址。

```text
cargo test -p rtd-rpc-api parses_shortened_owner_address --lib
1 passed; 0 failed
```

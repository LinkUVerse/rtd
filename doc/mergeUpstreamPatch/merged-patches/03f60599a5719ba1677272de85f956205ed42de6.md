# 03f60599：address block 解析期间保留 named-address map

- 上游提交：`03f60599a5719ba1677272de85f956205ed42de6`
- RTD 提交：`5f4694a150c3b185c75ec2e5f1287ab32de45953`

## 基线关联

fork 基线解析 address block 外层地址后未在 nested module 检查期间安装 named-address map，非法重复地址输入会在 `top_level_address` unwrap panic。

## 移植与验证

只移植 map 生命周期修复和一个 legacy regression，不移植同提交的 expect-message 清理。修复前 panic 于 `translate.rs:1032`，修复后返回 E02001。

```text
target/debug/rtd move build --path <regression> --environment testnet
before: called Option::unwrap() on a None value
after: E02001 duplicate declaration, no panic

cargo check -p rtd --bin rtd
Finished successfully
```

# b4862058：刷新被 module extension 扩展的 user module

- 上游提交：`b486205852707b2de41dfe62d76aa3299423cb02`
- RTD 提交：`c5f3fbe7830800a67fc4d1f0b4c3db73ff716c4c`

## 基线关联

fork 基线 Move analyzer 在 module extension 替换 user module 后未把 dependent modules 加回 dirty set，后续增量分析会保留指向旧 module 的 symbol 信息并崩溃。

## 移植与验证

在替换 extended user module 前收集其 dependents，并把这些 modules 与直接变更模块一起重新编译；移植完整 module-ext IDE fixture 与 snapshot。

```text
cargo test -p move-analyzer --test ide_testrtdte module_ext
1 passed; 0 failed
```

验证时为仓库中缺失 test target path 的嵌套 Move manifests 临时补充 `path = "tests/ide_testsuite.rs"`；测试后已撤销这些非补丁改动。

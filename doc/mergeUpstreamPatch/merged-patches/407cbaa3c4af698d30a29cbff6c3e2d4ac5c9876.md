# 407cbaa3：验证连续 default backoff delays

- 上游提交：`407cbaa3c4af698d30a29cbff6c3e2d4ac5c9876`
- RTD 提交：`16a5b98d52d5e56839581d63eb64963338301f6d`

## 基线关联

fork 基线测试将两组 bounds 与 `backoff.next()` 返回的单个 `Option` 进行 `zip`，迭代最多一次，第二个 backoff delay 从未验证。

## 移植与验证

逐个 bounds 调用 `next().unwrap()`，使两次连续 delay 都受范围断言覆盖。

```text
cargo test -p linku-common test_exponential_backoff_default --lib
1 passed; 0 failed
```

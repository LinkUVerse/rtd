# 上游提交审查批次 1701-1750

本批次回移了第 1735、1738 条共识活性修复和第 1744 条安全依赖修补。系统消息在高负载下不再被用户交易提交信号量阻塞；提交处理器以原子 in-flight 集合拒绝并发重复交易，并在请求结束后将摘要转入短期缓存；根工作区和 Move 子工作区的 `anyhow` 均已升级至 1.0.103。

`quick-xml` 的两个公告没有与现有 `object_store` 兼容的升级路径，且当前仅用于云存储服务响应解析，已在安全检查配置中做临时、带理由的忽略。第 1743 条使用的 TideHunter 修订与 RTD 当前 fork 不同，不能直接替换。

第 1703-1708、1715、1719-1720、1726、1746-1750 依赖后置 RPC 存储、KV RPC 或查询架构；第 1709、1711-1712、1716-1717、1722-1723、1727、1732、1736-1737 属于后续功能或协议改动。其余提交是文档、CI、测试、监控、日志或非关键重构，均未合入。

验证：`git diff --check` 通过。`cargo test -p rtd-core inflight_guard_tests --no-fail-fast` 在既有 `rtd-types::storage` 模块缺失处停止；`cargo metadata --locked --no-deps` 在根工作区和 Move 子工作区均通过。

第 1701-1750 条均已在上游提交清单中标记审查结论。

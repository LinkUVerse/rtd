# 上游提交审查批次 0701-0750

本批次仅保留直接修复 fork 基线已有 RTD 代码的关键缺陷。完整逐条判断见总账第 701-750 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 740 | \`9fbd4a495065d9134393c7b21bb36a904b7849fc\` | 发散/无效 match subject 与 native struct 解构被转为编译诊断，避免 match analysis 和 HLIR lowering ICE。 |

其余条目属于 fork 后的 protocol、address-balance、accumulator 路径，或是 Move/GraphQL/CLI 功能、性能优化、文档、CI、benchmark 和测试；均未合并。

## 验证

- 目标 Move 文件通过定向 \`rustfmt --check\` 与 \`git diff --check\`。
- \`cargo check -p rtd-core --lib\` 已重新编译 \`move-compiler\`，之后仍因既有 \`crates/rtd-types/src/lib.rs\` 声明而缺失的 \`storage\` 模块失败；该基线问题未修改。

第 701-750 条已全部标记为 \`APPLIED\` 或 \`REVIEWED\`，无 \`UNREVIEWED\` 残留。

# 上游提交审查批次 1301-1350

本批次只合入直接修复 fork 基线既有 RTD 代码的关键缺陷。

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 1333 | `ec11083fc1ca887fce90bb4c5069bfd1b467de88` | batch 验签失败后剔除坏签名，如有效签名仍达 quorum 则立即重新聚合，避免错误地丢失 quorum。 |
| 1347 | `ec11083fc1ca887fce90bb4c5069bfd1b467de88` | RPC index bulk 初始化后的 reopen 为全部列族恢复 `default_table_options`，避免 RocksDB 过早触发写入停滞。 |

第 1305、1306、1309、1311、1324、1335、1340、1348 条依赖 fork 后 coin-reservation、address balance、gasless、fungible stake 或 tidehunter；其余为功能、性能、文档、CI、镜像、版本、测试或非关键 CLI/RPC 路径，均未合并。

## 验证

- 局部格式与差异检查通过；两处文件存在补丁前已有的 import 排序提示，未重排无关代码。
- `cargo check -p rtd-core` 在编译 `rtd-types` 时受既有 `rtd-types::storage` 缺失模块阻断，未显示本批补丁错误。

第 1301-1350 条已全部标记为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。

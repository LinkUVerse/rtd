# 上游提交审查批次 1651-1700

本批次回移了第 1679、1680、1690、1691、1698 条共识提交关键修复：已被共识处理的交易不再重复提交；软批拒绝重复交易；软批在途数量按实际交易数计入限制；短时间内的重复提交被抑制；处理竞态返回可重试状态而不返回无效的共识位置。

这些修复仅适配既有的 RTD 软批和共识提交流程，新增错误、节点配置和回归测试均使用 RTD 命名。

第 1651、1655、1658-1660、1667-1678、1681-1683、1685-1688、1692-1695、1700 属于后续功能；第 1656、1657、1664、1665 依赖后置存储、查询或依赖版本架构。其余条目是文档、CI、监控、调试工具或非关键清理，均未合入。

验证：`git diff --check` 通过。`cargo test -p rtd-core test_duplicate_submission_suppressed_within_window --no-fail-fast` 在既有 `rtd-types::storage` 模块缺失处停止；随后 `rtd-rpc-api` 构建脚本因该失败读取到不完整的元数据。定向格式检查仅报告既有导入排序差异。

第 1651-1700 条均已在上游提交清单中标记审查结论。

# 上游提交审查批次 1251-1300

本批次只合入直接修复 fork 基线既有 RTD 代码的关键缺陷。

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 1260 | `8f632a8eebd1b11ef6e18b4af0522c1fdddb5692` | 将 divergent type 先解析为 Void，避免 `vector<void>` 触发 Move 编译器 ICE。 |
| 1261 | `8f632a8eebd1b11ef6e18b4af0522c1fdddb5692` | lexer lookahead 将词法错误延后给正常诊断路径，避免多行十六进制字面量解析 crash。 |
| 1273 | `a9b764e701882ed597eb851a9851fad2a01cac49` | `safe_range_iter` 的 exclusive maximum lower bound 不再泄漏最大键。 |
| 1285 | `d860750009c968a100c6a0db1353b97f008a6c0c` | object inclusion proof 在 V1 effects 上返回 precondition 错误，不再触发 abort。 |
| 1288 | `d860750009c968a100c6a0db1353b97f008a6c0c` | simulate resolver 在扫描、包读取前强制 PTB 结构上限。 |
| 1290 | `d860750009c968a100c6a0db1353b97f008a6c0c` | GetCheckpoint 多事件 JSON 渲染共享 16 MiB 默认预算，避免内存放大。 |
| 1291 | `b37b515fbf73902e36a4c09c72724d6f0d9ea85e` | gRPC 指标只将允许的方法路径作为标签，未知方法回落到匹配路由，阻断标签基数内存耗尽。 |

第 1255、1270、1275、1279、1286、1293、1297、1298、1300 条依赖 fork 后 gasless、address balance、coin-reservation、tidehunter 或 protocol v123/v124/v125。其余条目属于功能、性能优化、依赖/镜像、文档、CI、测试或非关键模拟路径，均未合并。

## 验证

- 第 1273 条：两个新回归测试先在旧实现在最大键边界失败；修复后 `cargo test -p typed-store --lib test_safe_range_iter_exclusive_lower_bound_at_max -- --nocapture` 通过（2/2）。
- 第 1260、1261 条：局部格式与差异检查通过；`cargo check -p rtd-core` 完成 `move-compiler` 编译，随后仅被既有 `rtd-types::storage` 缺失模块阻断。
- 第 1285、1288、1290、1291 条：局部格式与差异检查通过；`cargo test -p rtd-types ...` 和 `cargo test -p rtd-rpc-api ...` 均在既有 `rtd-types::storage` 缺失模块处停止。

第 1251-1300 条已全部标记为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。

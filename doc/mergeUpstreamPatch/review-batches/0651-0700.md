# 上游提交审查批次 0651-0700

本批次仅保留直接修复 fork 基线已有 RTD 代码的关键缺陷。完整逐条判断见总账第 651-700 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 658 | \`dc590235f2e2f1d2377d1316d44e9a55f3dfe581\` | RocksDB 安全迭代器将键/值反序列化失败返回 \`TypedStoreError\`，不再静默跳过损坏条目。 |
| 666 | \`3c5d943174fdc505f77896b735b96ac8c3727145\` | 无效声明字段继续生成诊断并保持稳定顺序，避免 match/Pack lowering ICE。 |
| 671 | \`b22a60570b9447f9d8a0f35325a2ab6650b7808e\` | guarded wildcard 后出现具体模式时继续特化，避免错误提前选择 wildcard 分支。 |
| 688 | \`b05e650da3a858b030a80d7dc466dcc8807a31e5\` | 系统对象输入改为默认拒绝，只允许显式列出的可变/只读组合。 |

其余条目属于 fork 后的 protocol、checkpoint split、address-balance、accumulator settlement、Tidehunter、KV/索引器路径，或是功能、性能调参、文档、CI、benchmark 与测试；均未合并。第 685 条模拟交易 panic 修复已核实为 RTD 当前代码中不存在的不安全调用，故无需重复移植。

## 验证

- 第 658 条新增回归测试通过：\`cargo test -p typed-store safe_iter_returns_error_on_deserialization_failure --lib\`（1 passed）。
- 第 666、671 条目标 Move 文件通过定向 \`rustfmt --check\`，并且 \`cargo check -p rtd-core --lib\` 重新编译通过 \`move-compiler\`。
- 第 688 条目标格式检查仅显示既有 import 排序差异；\`cargo check -p rtd-transaction-checks --lib\` 被既有 \`crates/rtd-types/src/lib.rs\` 声明而缺失的 \`storage\` 模块阻断，未修改该基线问题。
- 所有代码补丁均通过 \`git diff --check\`。

第 651-700 条已全部标记为 \`APPLIED\` 或 \`REVIEWED\`，无 \`UNREVIEWED\` 残留。

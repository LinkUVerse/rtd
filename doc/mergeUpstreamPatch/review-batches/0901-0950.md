# 上游提交审查批次 0901-0950

本批次仅保留直接修复 fork 基线已有 RTD 代码的关键缺陷。完整逐条判断见总账第 901-950 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 940 | \`75d144e1dd5d6ef91e03306e1c4214bfa297db40\` | 外部 resolver 名称拒绝路径分隔符，避免不安全地传入 \`Command::new\`。 |

第 945 条依赖上游后续的 \`transaction_rewriting\` MVCC replay 路径；RTD 当前不存在该模块或调用点，未作强行移植。

其余条目属于 fork 后 KV/address-balance/coin-reservation、执行引擎、索引器、Tidehunter 或 gasless 路径，或是功能、文档、CI、测试和非关键展示修复；均未合并。

## 验证

- 目标 Move package 文件通过定向 \`rustfmt --check\` 与 \`git diff --check\`。
- 上游回归测试存在，但外部 Move 工作区被既有缺失的 \`testrtdte\` 测试目标阻断，未进入测试执行；该清单问题未修改。

第 901-950 条已全部标记为 \`APPLIED\` 或 \`REVIEWED\`，无 \`UNREVIEWED\` 残留。

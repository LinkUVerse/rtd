# 上游提交审查批次 1051-1100

本批次仅合入两项直接修复 fork 基线已有 RTD 代码的关键缺陷。

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 1056 | `c47411de04551df89c853d456e8913b6c8fd61e0` | gRPC metrics response 缺少或携带非法路径头时改用安全占位值，避免服务端 panic。 |
| 1088 | `1146f2889f7edaf43eb0048d89315dbd72742b6b` | 修复 bridge action URL 拼接，并将请求限制交给框架 body limit，纳入审计反馈。 |

第 1055 条依赖协议 v121 的 Receiving 类型 gate；第 1071、1080、1087 条依赖后续 address-balance 或 BigTableConnection 架构。其余条目属于后续功能、Indexer-alt、framework/工具演进，或只影响文档、CI、测试和非关键路径，均未合并。

## 验证

- 第 1056 条：`cargo check -p linku-network` 通过，且目标文件通过定向 `rustfmt --check` 与 `git diff --check`。
- `cargo test -p linku-network --lib` 的 19 项中 18 项通过；`anemo_connection_monitor::tests::test_connectivity` 在沙箱绑定网络资源时返回 `Operation not permitted`，与该补丁无关。
- 第 1088 条目标文件通过定向格式与差异检查；`cargo check -p rtd-bridge` 在共享构建目录锁上未能取得完整结束输出，未将此视为通过。

第 1051-1100 条已全部标记为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。

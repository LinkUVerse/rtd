# 上游提交审查批次 1201-1250

本批次只合入直接修复 fork 基线既有 RTD 代码的关键缺陷。

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 1210 | `d19591a831424bea820cd58224493d101a013829` | 仅回移 Snappy 解压输出上限，防止压缩输入触发无界内存分配；不引入 anemo 版本升级或新的帧策略。 |
| 1229 | `db3ae21d1c8d6489d860358e6dfbd969b9ce9870` | 损坏的 RPC balance index 数据改为记录错误并停止 merge，避免节点 panic。 |
| 1238 | `c10d089f915d8f8d69970befb29536d74beac02b` | 修复带 guard 的 wildcard match arm 绑定处理。 |
| 1239 | `c10d089f915d8f8d69970befb29536d74beac02b` | 阻止错误模式的类型替换泄漏到 or-pattern 分支。 |
| 1240 | `c10d089f915d8f8d69970befb29536d74beac02b` | 错误 match 类型不再导致 Move 编译器 ICE/panic。 |
| 1248 | `d73a8516919872ad0fc9f3c23162a1c193d5c776` | 修复 RocksDB safe range iterator 丢弃最大编码键的 inclusive upper bound 条目。 |

第 1204、1207 条依赖 fork 后 coin-reservation 协议；第 1246 条依赖上游新增 gas model 12 与 protocol v123。GraphQL streaming、forking、Block Streaming、Move IR/反编译显示、索引器、镜像、性能优化、文档、CI、测试和非关键诊断改进均未合并。

## 验证

- 第 1210 条：`cargo test -p linku-network bounded_snappy_decompression_respects_output_limit -- --nocapture` 通过（1/1）。
- 第 1248 条：新增测试先在旧实现在最大键边界失败；修复后 `cargo test -p typed-store --lib test_safe_range_iter_inclusive_upper_bound_at_max -- --nocapture` 与 `test_reversed_safe_iter_inclusive_upper_bound_at_max` 均通过（各 1/1）。
- 第 1238–1240 条：`cargo check -p rtd-core` 已编译 `move-compiler`，随后受既有 `rtd-types::storage` 缺失模块阻断；Move 专用测试同样在既有 `move-model` 缺失 `testrtdte` 测试目标时停止。
- 第 1229 条通过差异检查；同一 `cargo check -p rtd-core` 在进入 rtd-core 前被既有 `rtd-types::storage` 缺失模块阻断。

第 1201-1250 条已全部标记为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。

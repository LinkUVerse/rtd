# 上游提交审查批次 0501-0550

本批次仅接纳直接修复 fork 基线既有代码的关键缺陷；完整逐条结论记录在总账第 501-550 行。

## 已合入

| 上游条目 | RTD 提交 | 缺陷与验证 |
| --- | --- | --- |
| 501 | `b55a13216d49e7e1f9738e0466cecf7945e46a13` | 基线已有 `rtd-kv-rpc` 通过 `gcp_auth` 使用 TLS 时未选择 rustls provider，启动可 panic。`cargo check -p rtd-kv-rpc` 编译至既有 `rtd-types::storage` 缺失模块后受阻。 |
| 524-526 | `3ca5eacf58d04d05bac9ddabc18ec778fa2358f6` | 修复基线 indexer-alt：背压前无限 spawn 可 OOM、并发 pipeline 不回传 watermark、DB 定时写入造成背压停滞。`cargo test -p rtd-indexer-alt-framework --lib` 在同一既有缺失模块后受阻；补丁 diff/格式检查通过。 |
| 544 | `aa5fc837d10dfe429dfd58a89e14a857dc2eb791` | 将既有节点默认 QUIC idle timeout 从 30 秒降为 10 秒，避免集体重启后 stale peer 使 checkpoint 同步停滞。`cargo check -p rtd-node` 在同一既有缺失模块后受阻。 |

## 排除原则

其余 45 条均已在总账标为 `REVIEWED`：文档、CI、日志、指标、测试/benchmark、依赖版本更新、功能/架构迁移一律排除；其中 529、531、540、549 等依赖 fork 后协议、ephemeral publication 或索引格式的提交标为 `EXCLUDE_POST_FORK`。

## 批次状态

第 501-550 条均已标记为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。本批次合入 5 个上游关键缺陷修复条目（524-526 共用一个 RTD 提交）。

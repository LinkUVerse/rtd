# 上游提交审查批次 1001-1050

本批次未发现可独立合并的、直接修复 fork 基线既有 RTD 代码的关键缺陷。

第 1005、1030 条依赖后续 consensus probe、periodic sync 与 block-sync 架构；第 1007 条依赖协议 v121 verifier gate；第 1043 条是横跨全仓库的新基础库 API 重构。它们不能作为最小关键补丁安全移植。

其余条目属于 fork 后 address-balance、coin-reservation、gasless、Indexer-alt、framework/工具功能，或只影响文档、CI、测试、benchmark 和非关键 CLI 路径，均未合并。

第 1001-1050 条已全部标记为 `REVIEWED`，无 `UNREVIEWED` 残留。

# 上游提交审查批次 0551-0600

本批次只保留直接修复 fork 基线既有 RTD 代码的关键缺陷；完整逐条判断见总账第 551-600 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 551 | `088efe7bfc5f5499d58b4c944adbd909fed7d3cb` | bridge 多地址 blocklist 解码的偏移错误。Foundry 未安装，Solidity 回归测试已写入。 |
| 564-568 | `937a08296afcb9fd5f0399b5e9065472172f25a1` | bridge 审计修复：禁用重定向、finality 竞态、错误详情泄露、过大响应与请求。`cargo test -p rtd-bridge --lib` 被既有 `rtd-types::storage` 缺失阻断。 |
| 593 | `8a6966dba24a9df5da3d28f16873fa799161d60f` | BigTable/Ledger 客户端固定 32 MiB gRPC 解码上限，避免大响应解码失败；未合入上游新增配置 API。定向检查被同一既有缺失模块阻断。 |

其余条目为 RPC/CLI/协议功能、后 fork schema 或执行路径、性能架构迁移、文档、CI、日志、指标、benchmark 或测试，均已排除。

第 551-600 条已全部标为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。

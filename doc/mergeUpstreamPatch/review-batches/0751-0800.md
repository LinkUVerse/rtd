# 上游提交审查批次 0751-0800

本批次仅保留直接修复 fork 基线已有 RTD 代码的关键缺陷。完整逐条判断见总账第 751-800 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 757 | \`d6abebdffcbae3ffbfd075e4dc450f888f618057\` | 常量依赖图忽略跨模块常量，并诊断单节点自引用，避免常量展开 ICE。 |
| 761 | \`aaceea141c94b2a1d8f4669d0be7557fccb6e71b\` | RTD mode object pack 字段缺失时安全退出 id-leak 分析，避免可达 unwrap。 |
| 762 | \`90d506e37eb33ba2937c82d0983c8bb5eb6e4280\` | 借用检查器将错误输入的硬断言改为诊断与恢复，避免多个 ICE。 |
| 779 | \`00b636e6549304eaf171b54a17435f517e62cf40\` | native struct 或缺失字段元数据的 match 诊断并安全失败，避免 specialization/compilation unwrap。 |

其余条目属于 fork 后的 address-balance、Display v2、协议和异步后处理路径，或是功能、性能调参、文档、CI、benchmark 与测试；均未合并。第 800 条依赖上游新 Tidehunter LOCK 文件协议，RTD 当前 Tidehunter 依赖未提供该语义，故未孤立移植。

## 验证

- 所有代码补丁均通过 \`git diff --check\`；第 757、762、779 条目标文件通过定向 \`rustfmt --check\`。
- 第 761 条的格式检查仅显示既有 import 排序差异。
- 每项 Move 修复均通过 \`cargo check -p rtd-core --lib\` 的 \`move-compiler\` 重新编译；完整检查随后仍被既有缺失的 \`rtd-types::storage\` 模块阻断。

第 751-800 条已全部标记为 \`APPLIED\` 或 \`REVIEWED\`，无 \`UNREVIEWED\` 残留。

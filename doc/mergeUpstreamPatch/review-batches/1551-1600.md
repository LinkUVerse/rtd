# 上游提交审查批次 1551-1600

本批次回移第 1586 条多 epoch 重配后的交易重试修复、第 1593 条签名二进制反序列化一致性修复，以及第 1600 条地址余额结算期间的 MVCC 一致性修复。第 1600 条将普通最新余额读取与根版本一致的调度读取分离，避免部分结算写入后的错误余额判断，并增加了回归场景。

第 1551-1585 条主要是反编译器功能和测试；第 1561 条依赖后置 DKG 路径；第 1587、1595、1599 是非关键模拟、脚本或指标调整。第 1596 的上游依赖修补针对 diesel 2.3.9，而 RTD 当前锁定 2.2.3，不能直接套用。其余提交均为文档、CI、工具或后续索引功能，未合并。

验证：针对第 1600 条的新回归测试及针对第 1593 条的既有签名序列化测试均已尝试运行，但在既有 `rtd-types::storage` 缺失处停止，未进入本批补丁代码。

第 1551-1600 条已全部标记为 `REVIEWED` 或 `APPLIED`，无 `UNREVIEWED` 残留。

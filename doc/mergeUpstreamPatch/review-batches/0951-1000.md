# 上游提交审查批次 0951-1000

本批次只保留直接修复 fork 基线已有 RTD 代码的关键缺陷。完整逐条判断见总账第 951-1000 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 961 | `96ceddc3d4c09cd0519b74f3238e55226932a77d` | 将生产 Move native 的不变量 unwrap/assert 转为可返回的 VM 错误，阻止无效输入使节点 panic。 |
| 975 | `312e84aa4992e8edac45df3a14644ad77c6e2bb5` | 固定 path-to-regexp 与 picomatch 的高危依赖版本，并更新 pnpm 锁文件。 |

第 973 条的 Bigtable table prefix 已采用正确的单斜杠格式。第 974 条针对新 Move VM direct-Value 入口的检查，在 RTD 当前旧 VM 的 serialized-call 路径中已有等价参数数目校验；第 984 条 release-only import 问题也不存在。

其余条目属于 fork 后 address-balance、coin-reservation、gasless、Indexer-alt、框架/工具功能，或仅影响文档、CI、测试和非关键展示路径，均未合并。

## 验证

- 第 961 条目标 Rust 文件通过定向 `rustfmt --check` 和 `git diff --check`。
- `cargo check -p rtd-move-natives-latest` 与 `cargo xclippy` 都编译到相关 Move/native crate，随后被既有 `crates/rtd-types/src/lib.rs` 缺失 `storage` 模块阻断。
- pnpm 锁文件离线校验因本机 store 缺少 `pnpm@9.1.1` 而未启动；未改动安装状态。

第 951-1000 条已全部标记为 `APPLIED` 或 `REVIEWED`，无 `UNREVIEWED` 残留。

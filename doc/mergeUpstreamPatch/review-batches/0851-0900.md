# 上游提交审查批次 0851-0900

本批次仅保留直接修复 fork 基线已有 RTD 代码的关键缺陷。完整逐条判断见总账第 851-900 行。

## 已合入

| 条目 | RTD 提交 | 修复 |
| --- | --- | --- |
| 867 | \`f2a1e33e742d122718fb4345dbe93c509d73ffe7\` | \`X_RTD_CHAIN_ID\` 使用完整 32 字节 digest，避免返回简写链 ID。 |
| 896 | \`b72f713bfd653327e3e6c35ebc7054751814a6d3\` | 桥同步在 RPC \`-32005\`/结果过多时减半查询窗口并保留游标重试，避免永久失败。 |

其余条目属于 fork 后的 snapshot/Tidehunter 协议、Block Streaming、address-balance、execution v3、Display、索引器或 protocol v118+ 路径，或是功能、性能、文档、CI、benchmark 和测试；均未合并。

## 验证

- 两项补丁均通过 \`git diff --check\` 与定向 \`rustfmt --check\`。
- \`cargo test -p rtd-bridge --lib\` 和 \`cargo check -p rtd-rpc-api --lib\` 都推进到目标包构建，随后被既有 \`rtd-types::storage\` 缺失阻断；该基线问题未修改。

第 851-900 条已全部标记为 \`APPLIED\` 或 \`REVIEWED\`，无 \`UNREVIEWED\` 残留。

# 上游提交审查批次 1801-1819

本批次回移了第 1802 条构建兼容修补和第 1817 条 RPC 可用性修复。前者将根工作区、Move 子工作区和三个示例锁文件中的 `ethnum` 固定到 1.5.3，修复 Rust 1.97 及以上版本的编译失败；后者将同步交易模拟移入 Tokio 阻塞线程池，避免耗时模拟占用异步运行时工作线程，并将阻塞任务 panic 转换为内部 RPC 错误。

第 1804 条依赖后置 KV RPC 路径；第 1818 条依赖 fork 后才引入的准入队列。第 1810、1816 条分别是非关键错误文案和未说明关键缺陷的常规依赖更新。其余提交属于索引接口、Move 升级、框架、IDE、CLI、压测、测试或文档，均未合入。

验证：`git diff --check` 通过；根工作区和 Move 子工作区的 `cargo metadata --locked --no-deps` 均通过。`cargo test -p rtd-rpc-api simulation_work_uses_the_blocking_pool --no-fail-fast` 已开始编译，但在既有的 `rtd-types::storage` 模块缺失处停止，测试体未执行。

第 1801-1819 条均已在上游提交清单中标记审查结论。

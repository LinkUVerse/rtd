# 上游提交审查批次 1501-1550

本批次回移了五条直接修复 fork 基线既有路径的关键补丁：第 1510、1513 条修复地址余额燃料结算下溢，并以 RTD 协议版本 106 安全启用；第 1544 条使懒 gRPC 通道遵从连接超时；第 1545、1546 条在异常重启后的 RPC 索引重建中避免余额重复累计；第 1549 条拒绝不同燃料价格的软交易包。

第 1518、1521、1523 是依赖上游后续早退出执行语义的跟进调整，关键下溢问题已由第 1510、1513 条覆盖。第 1524、1532、1543 依赖 fork 后的 DKG 或 tidehunter 路径；其余提交为后续功能、性能与代码重构、文档、CI、测试或非关键工具改动，均未合并。

验证：`cargo check -p linku-network` 与协议 106 开关单元测试通过；RPC 索引回归测试和核心检查均在既有的 `rtd-types::storage` 缺失处停止，未进入本批补丁代码。

第 1501-1550 条已全部标记为 `REVIEWED` 或 `APPLIED`，无 `UNREVIEWED` 残留。

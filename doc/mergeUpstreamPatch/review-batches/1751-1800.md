# 上游提交审查批次 1751-1800

本批次回移了第 1771 条共识块订阅竞态修复和第 1781 条依赖安全修补。前者在读取本地快照前先建立订阅，并在每次重连前重新计算已接收轮次，避免块发布与订阅并发时遗漏或重复回放；后者将根工作区、Move 子工作区和示例锁文件中的 `crossbeam-epoch` 升级至 0.9.20，修补指针解引用安全公告。

第 1752-1755、1757-1764 和 1796 依赖后置 RPC 存储路径；第 1765 依赖后置终态与丢弃模型；第 1786、1795 依赖后续协议版本开关和对象资金记录功能。其余提交为 SDK、GraphQL、CLI、索引、协议配置、快照、代理、测试、文档、CI 或非关键清理，均未合入。

验证：`git diff --check` 通过；根工作区和 Move 子工作区的 `cargo metadata --locked --no-deps` 均通过。`cargo test -p consensus-core subscriber --no-fail-fast` 已完成编译并开始执行，但测试环境未能分配可用端口，未能完成断言。

第 1751-1800 条均已在上游提交清单中标记审查结论。
